# Show Last Commit Differences

To see the differences between the last but one commit and last commit, simply use:

```
git show
```

And to also include the differences between last commit and current state:

```
git diff HEAD~
```

